package apitest

import (
	"testing"
)

func TestListForestDB(t *testing.T) {
	// testutils.Initialize(testutils.AppNameKecore)
	// defer testutils.Close()
	// ctx := testutils.NewCtx(testutils.WithUin(384))

	// dbplugins.

	// t.Log(logs.JSON(resp))
}
