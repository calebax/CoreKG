PROMPTS = {}

# ===================== 图片描述 =====================
PROMPTS["image_description"] = """## 角色
你是一个专业的视觉数据分析师。

## 目标
分析图像并提供其内容的全面描述。如果是图表，提取数据并分析趋势；如果是流程图/架构图，说明结构逻辑；如果是一般图片，描述视觉元素。

## 文档上下文
{context}

## 任务
1. 描述视觉表示的整体结构，指明它是图表、图形、表格、流程图、架构图、照片还是其他类型。
2. 如果存在轴、图例、标题或标签，识别并提取其中的文本。
3. 如果有数据可视化元素（柱形高度、折线坐标、饼图段、表格行列），提取关键数据点。
4. 分析并解释数据中显示的趋势、对比或模式。
5. 结合上下文信息理解图像的作用和意义，明确说明图像如何支持或验证上下文中提到的内容。
6. 保持客观准确，不要添加图像中不存在的内容，不要过度解读。
7. 使用原始语言，保持图像中文字的原始语言，不要翻译。
8. 输出为一段完整连续的纯文本描述，不使用 Markdown 格式标记。

## 注意
- 仅包含图像中明确存在的细节。如果某个元素不存在或不可见，不要提及。
- 如果图像内容缺失或无法理解，返回空字符串。
"""

# ===================== 表格描述 =====================
PROMPTS["table_description"] = """你是一个数据分析师。请根据表格内容和上下文信息，用一句话简短概括该表格的主题与用途。

## 上下文
{context}

## 表格内容
{table}

## 规则
1. 一句话，不超过50字，清晰说明表格内容及使用场景；
2. 结合上下文定位表格，例如"第三章的实验结果对比表"；
3. 只输出描述文本，不加解释或格式。
"""

# ===================== 大模型切块合并决策 =====================
PROMPTS["chunk_merge"] = """你是一个文档分析专家。下面是一篇文档按句子切分后的段落列表，每个段落用编号标记。
请判断哪些连续的段落属于同一语义主题，应该合并为一个 chunk。

## 规则
1. 语义紧密相关、讨论同一主题的连续段落应合并
2. 主题切换时（如从背景转入方法、从方法转入结论）应断开
3. 不要输出 chunk 内容本身，只输出合并决策
4. 输出格式：每行一个 chunk，用段落起止编号表示，如 "1-3" 或 "4"（单段独立）

## 段落列表
{segments}

## 输出（只输出合并决策）
"""

PROMPTS["chunk_merge_continue"] = """继续分析如下段落，结合前面的上下文判断合并边界。

## 已有上下文摘要
上一批最后一个 chunk 的主题：{context}

## 段落列表
{segments}

## 输出（只输出合并决策）
"""

# ===================== 自动模式选择 =====================
PROMPTS["auto_select_mode"] = """你是一个文档分析专家。请阅读以下文档片段，判断最适合的切块策略（chunk split mode）。

## 可选策略
- smart: 通用智能切分，适用于大部分结构化 Markdown 文档
- basic: 基础 token 切分，适用于无标题的纯文本
- advanced: 高级动态切分，适用于长短不一的混合文档
- title: 按标题级别严格切分，适用于有明确层级标题的文档
- slide: 按页切分，适用于 PPT/演示文稿（页面短小、内容离散）
- resume: 全文不切分，适用于简历等短文档（通常 1-2 页）
- paper: 论文切分，摘要独立 + 按章节标题切分
- laws: 法律条文切分，识别编/章/节/条层级
- llm: 大模型语义切分，适用于需要深度语义理解的复杂文档

## 文档片段
{sample}

## 判断标准
1. 如果文档每段以 "第X编"、"第X章"、"第X条"、"第X节" 开头 → laws
2. 如果有 "Abstract"、"摘要"、"引言"、"参考文献"、"References" 等学术结构 + 公式代码 → paper
3. 如果文档很短（片段本身已覆盖大部分内容）、简历格式 → resume
4. 如果每页只有简短标题+少量文字的幻灯片风格 → slide
5. 如果有清晰的多级标题层级（H1-H6），结构为章节体 → smart 或 title
6. 如果是纯文本无标题 → basic
7. 如果段落长短悬殊、格式混合 → advanced
8. 如果文档结构非常复杂、专业领域 → llm
9. 不确定时默认 smart

## 输出
只输出一个策略名称（如 "smart"），不要解释。
"""


