# AGENTS.md

CoreKG（又名 `roc`）— Go 单体仓库，知识库 / RAG 对话服务。知识平台（Forest = 知识库，File = 文档，Chat，Search）及配套内部运营服务。

## 关键事实

- **Go module 路径为 `github.com/insmtx/corekg`**（module 后缀与目录名 `corekg` 一致）。所有内部 import 使用 `github.com/insmtx/corekg/...` 前缀。Git remote 仍为 `git.yygu.cn:corekg/roc.git`（未随 module 重命名变更）。
- **单 Go module**（一个 `go.mod`，无 workspace）。多应用单体仓库，非多 module。
- **依赖已 vendor**（`vendor/` 已提交）。构建使用 vendor 模式。不要随意执行 `go get`/`go mod tidy`；若修改依赖，需执行 `go mod tidy && go mod vendor` 并提交 `vendor/`。
- 克隆仓库后执行 `git config pull.rebase true`（README 要求）。

## 仓库结构

- `apps/<name>/` — 可部署服务。每个应用有 `cmd/main.go`（cobra 入口）和 `app.go`，暴露 `Routers(*server.Router)`、`Migrates(*gorm.DB)`、`RunJob(...)`。可构建应用：account、admin、cook、corekg、keapi、kechat、kecore、keinit、keparser、kesearch、keworker。
- `apps/corekg/` — **聚合单体**：其 `Routers` 挂载 kecore + kechat + account + keapi + keparser + kesearch。修改子应用路由会同时影响其独立二进制和 corekg。
- `pkgs/` — 共享库（`global` = 共享常量/错误码/环境变量，`einotools` = Eino ReAct/Summary agents，`task`/`queue`/`jobs`，`utils` 等）。
- `clients/` — 独立客户端二进制（如 Windows `puppyui`）。
- `scripts/mysql/` — 版本化 SQL 迁移脚本。`scripts/mysql_saas/`、`resource/`（含 `locales/` i18n）、`version/`（ldflags 注入的构建信息）。

## 应用分层（每个应用）

`internal/apis/apis.go`（路由注册）→ `internal/apis/<ctl>` handler → `services/svc*` → `models/*`（GORM）。DTO 在 `internal/dto`。`internal/docs` 为 **swag 生成的 swagger 文档**（禁止手动编辑）。`internal/middleware`、`mds`/`accountmds` = 中间件/共享 model 辅助。`conf/<env>/config.yaml` 存放各环境配置。

路由使用内部 `yg-go` 框架，基于 action 而非原始 gin 路径：
`eng.P("app.Action", ...)` = POST，`eng.G(...)` = GET，`eng.HandleDoc("app")` = swagger。action 字符串（如 `keapi.ListForest`）映射到 URL 路径。

## 命令

始终传入 `APP=<name>`（Makefile 默认值 `APP ?= roc` 已过时，不存在 `roc` 应用）。`ENV` 默认为 `test`。

- 构建本地二进制：`make local APP=keapi` → `bundles/keapi`
- 构建并运行：`make run APP=keapi ENV=test`（读取 `./apps/<app>/conf/<env>/config.yaml`）
- 构建 Linux 二进制：`make linux APP=keapi`
- 完整构建（文档 + 二进制）：`make build APP=keapi`
- 重新生成 swagger 文档：`make generate-docs APP=keapi`（自动安装 `swag`；写入 `apps/<app>/internal/docs`）
- 构建/推送镜像：`make push-image APP=keapi`（CI 使用）
- 测试：`make test` = `go test -v ./...`。建议定向运行，如 `go test ./apps/keapi/...`。

### 代码生成（私有工具）

`make codegen APP=<app> MODE=api,module,model` 使用私有仓库 `git.yygu.cn/pkg/yggocli` 的 `yggocli`。自动配置 `GOPRIVATE=git.yygu.cn` 和 SSH `insteadOf` 重写，需要 `apps/<app>/conf/<env>/code_gen.yaml`（含 `mysql_dsn`）。需 SSH 访问 git.yygu.cn，否则失败。

## 测试注意事项

- 测试**非隔离**：多数测试加载真实配置（如 `../conf/test.yaml`）并连接测试环境 MySQL / Elasticsearch / Redis。`go test ./...` 会发起网络请求，缺少这些服务时会失败或挂起。仅运行可满足依赖的包级测试。
- 本地基础设施：`make dev-mysql`、`make dev-es`；Redis/RedisGraph 通过 docker 启动（见 README）。ES 使用 `ik_max_word` 分词器；索引按公司/知识库命名为 `ke_0`、`ke_1` 等。
- CI **不阻断**测试或 lint（GitLab `unit-test` stage 为空壳；`make test` 已注释）。

## 代码风格 / 约定

- `.golangci.yml` 启用了极严格的 linter 集（exhaustruct、wsl、err113、wrapcheck、mnd、varnamelen、gochecknoglobals 等），但 CI 中**未运行**。匹配周围代码风格；不要期望遗留文件通过完整 linter 检查。
- Struct tag 大小写（tagliatelle）：`json` = snake_case，`yaml` = camelCase。导出符号需 doc comment（revive `exported`）。
- 除非被要求，不要添加注释。不要编辑生成文件（`*_docs.go`、标注 "Code generated by swaggo/swag" 的 `docs.go`）。

## MySQL 迁移脚本（`scripts/mysql/`）

- 文件名：`${version}_${seq}__${action}.sql`，全小写（如 `v1.6_1__create_table.sql`）。
- **已发布版本的脚本不可修改，仅执行一次。** 禁止编辑已发布脚本；通过新版本脚本实现变更或回滚。索引前缀：`uk_`、`idx_`。

## 部署 / CI

- 两套流水线：**GitLab CI**（`.gitlab-ci.yml`，web/api 触发：构建镜像 → SSH 部署 → 企微通知）和 **GitHub Actions**（`.github/workflows/*.yml`，`workflow_dispatch`，自托管 runner）。均调用 `make ... APP=<app>`，部署镜像 `example.com/corekg/<app>-api:<tag>`。
- README 中指向 `github.com/insmtx/corekg` Actions（ci.yml/release.yml）的 badge 为预期目标，对应 workflow 文件尚不存在。

## 领域参考

- `keapi` 同时提供 **MCP Server**，路径 `/v3/keapi/mcp`（StreamableHTTP），与 keapi HTTP 端口共用（默认 `:8086`），使用 API-Key 鉴权。21 个 Tool 及 curl 验证方式见 `README.md`。
- RAG/对话流程的深层架构（API → service → ChatWrapper 模式 → 8 步 rerank 管线 → Eino agents）记录在 `docs/core-business-flow.md`。修改对话/搜索内部逻辑前请先阅读。
</content>
</invoke>
